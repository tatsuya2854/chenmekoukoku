# 絵コンテ（構図ラフ）
- `A_storyboard.svg` / `B_storyboard.svg` / `C_storyboard.svg`：各 8 フレーム、9:16。赤帯は UI セーフゾーン外（上 220／下 430／右 120px）。
- ボトルは「正式画像」と書いた記号で表示。実制作では `assets/product/` の画像を改変なしで使う（ラベル内容はここに描かない）。
- 日本語フォントが無い環境では文字が □ になる。ブラウザ／Illustrator／Figma で開く。
- 再生成：`python3 04_scripts/storyboards/make_storyboards.py`
